{
  "name": "espeho-erp",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && cp -r frontend/dist backend/public",
    "start": "node backend/src/index.js"
  }
}
